#pragma once
#include "stdafx.h"
#include <map>
#include <string>
#include <sstream>
#include <iostream>
#include <iterator>
#include <vector>
#include <cassert>
#include <fstream>
#include <algorithm>

/*
typedef std::map<std::string, std::string> dictionaryType;

void InitDictonary(const std::string &file);
dictionaryType ReadWords(const std::string& file);
dictionaryType CreateReverseDictionary(const dictionaryType &dictionary);
std::string FindTranclate(const std::string &word,  dictionaryType &dictionary,  dictionaryType &reverseDictionary);
void AddTranslate(const std::string &word1, const std::string &word2, dictionaryType &dictionary, dictionaryType &reverseDictionary);
void SaveDictionary(const std::string &fileName, dictionaryType &dictionary);
void OutDict(dictionaryType dictionary);
*/


